# dataforge-cli
Python CLI that ingests datasets from varied sources/formats, infers schema, profiles/analyzes them, and renders structured outputs (CSV/JSON/YAML/HTML tables/charts) into HTML templates for static sites, PHP, or WordPress.
